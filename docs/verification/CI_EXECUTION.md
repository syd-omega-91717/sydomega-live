# CI execution marker

This file exists solely to trigger and document a fresh GitHub Actions CI execution on `main` after production-surface verification.

No application runtime behavior is changed by this marker.
