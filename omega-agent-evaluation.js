/* Ω AGENT EVALUATION — lightweight quality gates */
(function(global){'use strict';if(global.OmegaAgentEvaluation)return;function score(sample){sample=sample||{};var checks=[['facts',sample.facts_ok],['voice',sample.voice_ok],['brief',sample.brief_ok],['sources',sample.sources_ok],['review',sample.review_ok]];var passed=checks.filter(function(x){return x[1]===true}).length;return {score:passed/checks.length,passed:passed,total:checks.length,checks:checks.map(function(x){return {name:x[0],passed:x[1]===true}}),release:passed===checks.length};}function record(agent,sample){var result=score(sample);if(global.OmegaEvidence)OmegaEvidence.record({capability:'agent-evaluation',kind:'quality_gate',value:result.score,source:'omega-agent-evaluation',meta:{agent:agent,release:result.release}});if(global.OmegaProvenance)OmegaProvenance.add({source:'omega-agent-evaluation',type:'evaluation',claim:'quality gate for '+agent,confidence:result.score,metadata:result});return result}global.OmegaAgentEvaluation=Object.freeze({version:'1.0.0',score:score,record:record});})(window);
