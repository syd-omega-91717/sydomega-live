// ============================================================================
// FILE: /backend/src/modules/authorization/tests/oidc.integration.spec.ts
// NEW FILE
// ============================================================================

describe(

    "OpenID Connect Provider",

    ()=>{

        it("publishes discovery document",async()=>{});

        it("issues id token",async()=>{});

        it("returns userinfo",async()=>{});

        it("supports nonce validation",async()=>{});

        it("supports RP initiated logout",async()=>{});

        it("supports end session endpoint",async()=>{});

        it("supports standard OIDC claims",async()=>{});

    }

);
